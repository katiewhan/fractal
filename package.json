{
  "name": "dimension",
  "version": "1.0.0",
  "description": "Fractal Dimension Animation",
  "author": "Katie Han",
  "main": "src/index.js",
  "scripts": {
    "clean": "rm -rf ./dist",
    "build": "tsc --noEmit && rm -rf ./.cache && parcel build --out-dir ./dist --public-url ./ ./src/*.html",
    "dev": "parcel serve --out-dir ./dist ./src/*.html",
    "deploy": "gh-pages -d dist"
  },
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^14.14.25",
    "@types/p5": "^0.9.1",
    "gh-pages": "^3.1.0",
    "p5": "^1.2.0",
    "parcel-bundler": "^1.12.4",
    "typescript": "^4.1.3"
  }
}
